var isPrepared = true;
var isQueued = true;

// We can flip the boolean values using the "!" symbol.

if(!isPrepared){
console.log("The meal is ready");
} else if (isQueued) { 
console.log("Meal is queued for preparation");
} else { 
console.log("Order has been taken");
}